# Python bytecode files
*.pyc
*.pyo
*.pyd
__pycache__/

# Django migration files (ignore all migrations)
*/migrations/*

# Media files (if you store media files locally in your project folder)
media/

# Static files (if you're not committing the static folder, or if it's generated by `collectstatic`)
static/

# Virtual environment directory (use the name of your virtual environment directory if different)
venv/
env/

# Django settings files that might contain secrets
*.env